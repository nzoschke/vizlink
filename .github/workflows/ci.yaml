name: CI

on:
  pull_request:
    branches: ["main"]

jobs:
  build:
    name: ${{ matrix.os.arch }}
    runs-on: ${{ matrix.os.image }}
    strategy:
      matrix:
        os:
          - arch: darwin-arm64
            image: macos-latest
            zip: zip -j vizlink.zip vizlink/target/vizlink
          - arch: darwin-amd64
            image: macos-latest-large
            zip: zip -j vizlink.zip vizlink/target/vizlink
          - arch: windows-amd64
            image: windows-latest
            zip: Compress-Archive -Destination vizlink.zip -Path vizlink/target/vizlink.exe, vizlink/target/*.dll
          - arch: linux-amd64
            image: ubuntu-latest
            zip: zip -j vizlink.zip vizlink/target/vizlink vizlink/target/*.so

    steps:
      - uses: actions/checkout@v4

      - uses: graalvm/setup-graalvm@v1
        with:
          cache: "maven"
          distribution: "liberica"
          github-token: ${{ secrets.GITHUB_TOKEN }}
          java-version: "21"
          native-image-job-reports: "true"

      - run: |
          mvn install -f vizlink/pom.xml -q
          mvn package -f vizlink/pom.xml -q -Pnative -DskipTests

      - run: ${{ matrix.os.zip }}

      - uses: actions/upload-artifact@v4
        with:
          name: vizlink-${{ matrix.os.arch }}
          path: vizlink.zip
